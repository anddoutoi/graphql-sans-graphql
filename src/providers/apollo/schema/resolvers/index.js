import RootQuery from './root-query.resolver';
import Ninja from './ninja.resolver';
import Tribe from './tribe.resolver';

const resolvers = {
	RootQuery,
	Ninja,
	Tribe,
};

export default resolvers;
